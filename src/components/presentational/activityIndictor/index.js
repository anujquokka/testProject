import { connect } from 'react-redux';
import { default as AppActivityIndicator } from './AppActivityIndicator';
import {} from '../../../store';

const mapStateToProps = (state) => ({

});

const mapDispatchToProps = (dispatch) => ({
});

export default connect(mapStateToProps, mapDispatchToProps)(AppActivityIndicator);
